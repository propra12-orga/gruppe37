package Objects;

import javax.swing.JPanel;

public class Spieler extends JPanel {
	/*
	 * 
	 * private static final long serialVersionUID = 1L;
	 * 
	 * private final Spielfeld panel;
	 * 
	 * public static int ground = 0; public static int solid = 1; public static
	 * int breakblock = 2; public static int bombesetzen = 3; public static int
	 * spieler_bombe = 4; public static int explosion_mitte = 5; public static
	 * int explosion_horizontal = 6; public static int explosion_vertikal = 7;
	 * public static int spieler = 8; public static int versteckterausgang = 9;
	 * public static int ausgang = 10; public static int spieler2 = 11; public
	 * static int spieler2_bombe = 12;
	 * 
	 * public Spieler(Spielfeld parent) { panel = parent;
	 * 
	 * } /* public void moveRight() { boolean moveRight = Steuerung.moveRight;
	 * 
	 * int x = this.getX(); int y = this.getY(); int nextStatus =
	 * panel.getBlockStatus1(x + 1, y); int currentBlock =
	 * panel.getBlockStatus1(x, y);
	 * 
	 * if (moveRight == true && Spielfeld.player2alive == true && nextStatus ==
	 * ground || nextStatus == ausgang) {
	 * 
	 * if (nextStatus == ausgang) { // game_over.start(); } else if
	 * (currentBlock == spieler2_bombe) { panel.setBlockStatus1(x + 1, y,
	 * bombesetzen); } else { panel.setBlockStatus1(x + 1, y, ground); } x++;
	 * panel.setBlockStatus1(x + 1, y, spieler); System.out.println("bla" + y);
	 * } }
	 * 
	 * public void moveLeft() { boolean moveLeft = Steuerung.moveLeft;
	 * 
	 * int x = this.getX(); int y = this.getY(); int nextStatus =
	 * panel.getBlockStatus1(x - 1, y); int currentBlock =
	 * panel.getBlockStatus1(x, y);
	 * 
	 * if (moveLeft == true && Spielfeld.player2alive == true && nextStatus ==
	 * ground || nextStatus == ausgang) {
	 * 
	 * if (nextStatus == ausgang) { // game_over.start(); } else if
	 * (currentBlock == spieler2_bombe) { panel.setBlockStatus1(x - 1, y,
	 * bombesetzen); } else { panel.setBlockStatus1(x - 1, y, ground); } x--;
	 * panel.setBlockStatus1(x - 1, y, spieler); System.out.println("bla" + y);
	 * } }
	 * 
	 * public void moveUp() { boolean moveUp = Steuerung.moveUp;
	 * 
	 * int x = this.getX(); int y = this.getY(); int nextStatus =
	 * panel.getBlockStatus1(x, y - 1); int currentBlock =
	 * panel.getBlockStatus1(x, y);
	 * 
	 * if (moveUp == true && Spielfeld.player2alive == true && nextStatus ==
	 * ground || nextStatus == ausgang) {
	 * 
	 * if (nextStatus == ausgang) { // game_over.start(); } else if
	 * (currentBlock == spieler2_bombe) { panel.setBlockStatus1(x, y - 1,
	 * bombesetzen); } else { panel.setBlockStatus1(x, y - 1, ground); } y--;
	 * panel.setBlockStatus1(x, y - 1, spieler); System.out.println("bla" + y);
	 * } }
	 * 
	 * public void moveDown() { boolean moveDown = Steuerung.moveDown;
	 * 
	 * int x = this.getX(); int y = this.getY(); int nextStatus =
	 * panel.getBlockStatus1(x, y + 1); int currentBlock =
	 * panel.getBlockStatus1(x, y);
	 * 
	 * if (moveDown == true && Spielfeld.player2alive == true && nextStatus ==
	 * ground || nextStatus == ausgang) {
	 * 
	 * if (nextStatus == ausgang) { // game_over.start(); } else if
	 * (currentBlock == spieler2_bombe) { panel.setBlockStatus1(x, y + 1,
	 * bombesetzen); } else { panel.setBlockStatus1(x, y + 1, ground); } y++;
	 * panel.setBlockStatus1(x, y + 1, spieler); System.out.println("bla" + y);
	 * } }
	 * 
	 * 
	 * public void bomb() {
	 * 
	 * boolean bomb2 = Steuerung2.bomb2; } if (bomb2 == true &&
	 * Spielfeld.player2alive == true && Spielfeld.nextbomb2 == true) {
	 * nextbomb2 = false; blockStatus[x2][y2] = spieler2_bombe; a2 = x2; b2 =
	 * y2; zeichnen(); explosion2.start(); }
	 */

}
